# Следующий фокус развития GOYDA

**Зафиксировано сейчас:** линия **качество (C)** — в CI для job `frontend` добавлен шаг `npm run lint` (см. [`.github/workflows/ci.yml`](../.github/workflows/ci.yml)).

**Дальше по приоритету (выберите одно):**

| Приоритет | Направление | Идеи |
|-----------|-------------|------|
| Демо / UX | Продукт (A) | Карта Leaflet/OSM по полям `lat`/`lng` у локаций; фильтр списка по сезону; человекочитаемые сообщения об ошибках в SPA. |
| Документация | Контракт | Расширить [`api/openapi.yaml`](../api/openapi.yaml) схемами тел ответов (`User`, `Location`, `Route`, ошибки). |
| Стабильность | Качество (C) дальше | `go test ./...`, httptest для `/health` и публичных GET; при необходимости e2e (Playwright) поверх `localhost:8080`. |

Локальная проверка после релиза: [`local-smoke.md`](local-smoke.md).
